## Research Results: Convex backend AI agents

**Date Range:** 2026-04-09 to 2026-05-09
**Mode:** both
**OpenAI Model:** gpt-5.1-codex-mini

### Reddit Threads

*No relevant Reddit threads found for this topic.*

### X Posts

**X8** (score:63) @attaboiaj (2026-05-06)
  @CudeDesign @sudo_overflow @levelsio use convex it's much better with AI and it's real time updates are magical. it can def be better backend

How much you pay for monday monthly? 
How you monday can ...
  https://x.com/attaboiaj/status/2051890035889471750
  **

**X6** (score:63) @dukebiz (2026-05-06)
  Most solo builders waste their first year on the wrong stack.

Here's what I'd build with today.

Frontend: Next.js + shadcn/ui. Stop reinventing components. 

Backend: Supabase or Convex. Auth, DB, s...
  https://x.com/dukebiz/status/2052038173715517749
  **

**X11** (score:62) @clonkbot (2026-05-05)
  @donethedirt ✅ App live: https://t.co/UzEZ1pva5s
🤖 AI-powered + Convex real-time backend
🌐 https://t.co/ENJZoaTDwE
📝 Contribute: https://t.co/TkvBUzgQtA

💰 0 credits remaining — sign up at https://t.c...
  https://x.com/clonkbot/status/2051675310366584927
  **

**X16** (score:60) @ysnzro (2026-05-03)
  @thsottiaux Codex for some reason is consistently getting beaten on convex a typescript reactive backend that makes ai coding a breeze

https://t.co/RgaEMrmQko...
  https://x.com/ysnzro/status/2051065686563377578
  **

**X23** (score:59) @clonkbot (2026-05-02)
  @CryptoStacksss @grok ✅ App live: https://t.co/JwveBLWRCU
🤖 AI-powered + Convex real-time backend
🌐 https://t.co/pssNL5h3Av
📝 Contribute: https://t.co/CFByk7SHpV

💰 1 credits remaining — sign up at ht...
  https://x.com/clonkbot/status/2050564121888801212
  **

**X24** (score:59) @clonkbot (2026-05-02)
  @CryptoStacksss @grok ✅ App live: https://t.co/mN19g6Z0GH
🤖 AI-powered + Convex real-time backend
🌐 https://t.co/MT2R1voCgN
📝 Contribute: https://t.co/mrpuZeb6HK

💰 2 credits remaining — sign up at ht...
  https://x.com/clonkbot/status/2050563274215756156
  **

**X17** (score:54) @ysnzro (2026-05-03) [1likes]
  @luisvega_dev @convex AI is so good with it I pity the ones using any other backend...
  https://x.com/ysnzro/status/2051061293080346795
  **

**X30** (score:54) @waynesutton (2026-05-01) [1likes, 1rt]
  AI Stack by @alperortac

A community platform to share your AI setup and get inspired by what other builders are running in production - compare tools, costs, and workflows.

Built with Convex as self...
  https://x.com/waynesutton/status/2050244097697562704
  **

**X28** (score:32) @leodev (2026-05-01) [1likes]
  @NithinMonni Convex is the backend and database...
  https://x.com/leodev/status/2050253319675801951
  **

### YouTube Videos

**-AVtkiG0laE** (score:55) TubeGuruji (2025-11-08) [61,269 views, 714 likes]
  Build & Deploy AI Agent Builder Platform using NextJs, React, Convex, Arcjet
  https://www.youtube.com/watch?v=-AVtkiG0laE
  Highlights:
    - "Then for the database we are going to use a convex database which is a real time update database and very easy to integrate."
    - "Click enter and then you will see your application is running on URL localhost on a port 3000."
    - "Convex DB is one of the popular database among the developers as it provide a lot of different feature like realtime update type safety and you can easily integrate to any number of frameworks."
    - "Not only that but convex also give you the option to write a server functions store your files and the most important the chrome job functionality inside of one uh database."
    - "Everything you will get inside the convex and the most interesting thing is free to use as well."
  <details><summary>Full transcript (5000 words)</summary>
  With the help of React, NexJS and the power of AI, we are going to build an AI agent builder platform called agentify where user can simply build an AI agent using drag and drop. This is a fullstack SAS application which we are going to build completely from the scratch. Hey there and welcome back to Tube Guruji channel. Let me walk through this application first and then we'll talk about the all the text stack which we are going to use to build this application. This is a aentify app. Now simply in order to sign in click get started and then it will redirect you to the signin page. You have option to sign in with a Google or email and password. Once you sign in you will redirect it to a dashboard screen where you will see a sidebar. Obviously we are going to use a shad and this sidebar when you collapse you will see the icon. When we you expand this one, you will see a complete sidebar with the menu options and all. Pretty cool, right? Now, if you see, we already have some of the agent created. But if you want to create a new agent, simply click create and give the agent name. So, in this case, let's say you want to create weather agent and simply say create. After creating this agent, you will redirect it to a agent builder platform. If you see we have this beautiful u drag and hold functionality. Currently we have the default start node. It means this is the starting point of our agent. On the right left side you will see a different different agent tools available. I'm going to teach you how to create this each node how to create and customize this each of the node. And not only that but you can even add more feature more tool into your app. Now let's understand how it works. Now once you click any of this tool it that tool will get added to your platform. Simply drag it and you can even connect it these two nodes together by with the help of this aging. Pretty cool right? Once you select this agent on the right hand side you will see this setting panel. For that agent you can update your configuration. For example, if you want to give the agent name simply add the name. So here we'll say weather agent help to get the weather of any city. Now after that you have option to select a model. You can include a chat history and in the output format if you want a JSON format you can provide a JSON schema. If you want to keep the text, you can keep the text as it is and then save it. Once you save, you will see this beautiful toast message as well. That also we are going to learn how to add it. And whenever you think your agent look good, you can even click this save button. Now, if you want to add different kind of nodes like for example if else node. So once you add this if else node, you will see this different uh node design. Now you can add a condition or else condition and depends on your structure it automatically redirect it. For example on a weather node you can add a condition. If city is not equal to null it means city value is there then you can add an API which will connect to the weather API. Okay. So you can just drag it like this. But if the condition is false then you can just end the conversation. Okay. And user will ask to enter the city. Now for the end conversation you will see a different setting option. For a API as well you will see a different option. So here we will say weather API. You can have option to select a different request method like get. If you select the post it automatically add an option to enter the body parameter as well. Now in this case we want to add just get request. Now enter the API URL and the API key as well. For the API URL let me copy it first. I will copy this API key uh from the weather API and we'll paste it here. Then for the URL you can just get it from the documentation and here you can paste it. I will say current dojson question mark query is equal to and then we'll say city name. Okay. And once you are finished with this just save it and then save this one as well. And at the last if you want to end this conversation once you get the result you can connect to the end and you can add uh any specific output format you want you can just mention it here. But other than that we also have some few more tools like while loop which will uh ask the question again is this kind of while loop if you want to um keep certain task repetitive manner right you can even delete the node if you don't want also we have this user approval node and just you can add a condition if user want to approve or reject in the flow and you can add more option once you finish with this one. Just save this one and then you have option for a preview. Click on this preview button and then it will redirect you to the preview screen and on this preview screen you will see your complete flow. Here you cannot edit anything. You can just drag it or if you want to zoom you can just zoom this one but you cannot select any node because this is in the preview mode now. Okay. On on the right hand side currently it not generated any config. So you can say reboot the agent. This happen only first time. Whenever you make changes, make sure to reboot this agent and then your agent is now ready to use. Now over here you can just start conversation with the agent. Once you start you will see in a streaming format you will get the result. Let's ask some question. Can you get me weather info? and then it will uh reply you back and boom if you see it's saying okay let me provide the city now let's provide a city let's say Chicago and then it will fetch the real information directly from the API so here we'll say yes and then boom if you see we got live temperature from the Chicago and this is from the API directly if you want to get more weather let's say Pune city weather info and then send this data and you'll get in a streaming format see pretty cool right and everything we are fetching it from the API and that's how the agent works now let's consider that do you want to use this agent in your own application after building this one you can simply click publish option and boom you'll get this beautiful pre-made code. Simply copy this code, paste it in your project and then you'll start conversation with the uh agent which you created on this platform. If you want to make the changes, make sure you can add all the details you want. Save it and again go to the preview section here, reboot your agent and then it will work smoothly as expected. After creating your agent, you'll see your agent on the dashboard. Anytime if you want to edit, simply go to that and you will see your all the workflow and you can start editing. Now over here currently I am a free user that's why now we don't have any credits uh remaining. Okay, it will also track you the free credits. Now for a free user you can only create two agent but you have option to subscribe for unlimited agent creation. Simply go to the pricing section and here you added the pricing feature. The subscription feature which make our application SAS with the help of clerk subscription billing model within a just one line of code. We added this pricing model. Whichever plan you want to subscribe just click subscribe make a payment and then it automatically subscribe to your plan and then you will see now you can create unlimited agent now you don't have any limitation just you can create as many agent you want under the AI agent section you will see all the agent which you created and the profile section show you all of this information you can even manage your billing by going through this billing section over here you'll find all the details when you join your statements, your payment when you made it and you can even access everything from this um user profile author as well. Now everything we are going to build it completely from the scratch. Each feature I'm divided into a small small chapter so it will be easy for you to track and learn much easier way. At the end of this course, I also deployed this application on a cloud so that you can share this project with your friends and family. Now let's talk about the text stack which we are going to use to build this application. The very first aj which is the react framework to develop this web application. Then for the database we are going to use a convex database which is a real time update database and very easy to integrate. uh you can create the function directly inside the convex and there are lot of different feature you will find with this convex for rate limiting we are going to use arjet which help us to secure applica our application but not only that it help us to implement the rate limiting feature that that uh help us to minimize our lot of work and the last but not least the clerk authentication along with the clerk billing feature everything we are going to learn step by step and from the scratch if you are new to do to any of these tool then don't worry because I'm going to tell you each tool step by step so guys if you did not subscribe to our channel please do subscribe also join the tube gorgi pro membership in order to get an exclusive access to my all the sources uh source code courses and here you'll find each of the u project source code. Also, if you want to create an AI short film, then visit to kraixstudio.com. The link is in the description where you can just create an AI short film by just entering your prompt with a consistent character. You don't need to do anything. You can generate a a short film depends on your video style. Everything is taken care by the Kraix studio. You can even edit each scene one by one. You can even edit the characters, change the video style and many more options. Just visit the Kraic Studio now. The link is in in the description and get a big discount. So guys without doing any further delay let's begin to develop and agentify an AI agent builder platform. Now in this first chapter we are going to create a reactjs application. Then we'll learn how to run that application. Then we'll install the shad CN UI component tin cssbased library. and then we'll learn how you can change the application font as well. So let's go to this nexjs.org. NextJS is a React framework to develop the web application and it provide a lot of different feature. The very popular one is a folder based routing, a client server rendering and there are many others. Now simply open a folder where you want to create this project and then if you are using a Mac OS use a terminal. If you're using Windows make sure to open with the command prompt and simply type a command npx create next app at latest. Now at latest will install the latest version of the NexJS application. But if you want to install any specific version of the NexJS, simply enter that version number and then click enter. Next uh enter the project name. So here we say AI agent builder platform. Then uh say yes to the TypeScript. It is very recommended to use a TypeScript. Don't worry if you never used the TypeScript before. Okay? If you know the JavaScript then you can easily uh use the TypeScript as well. Then none to the lin yes to the Telvin CSS, no to the source directory, yes to the app router, no to the turbo pack and then no to the input allies. And once it is done, your project is ready. Then simply open a VS Code, open a folder where you just created this project and then click select that particular project and simply click open. Here if you get this popup you can just say yes I trust the author and then you will see some of the files and folder uh inside your project. Let me walk through each of these folder and files. The very first folder which is the app folder which contain all your routes pages all your code which you are going to write. The very first file which which is a global CSS which contain a globally applied stylesheet. Then we have the layout.tsx in which uh this is the root layout of your application. This is a starting point of your application in which it contains the uh font which is applied the metadata which is quite important for your uh SEO purpose. Then we have the HTML body tag and inside this the children are rendering. So all the routes are going to render through this children. Okay. Now here you can even update the metadata data. Let's say Here we'll say the app where you can build AI agent by simply drag and drop. Okay, you can uh update this title description or you can use chat GPT uh to write your metadata. Once this is done, the next p uh file is a page.tsx which is the default page of your application. Now once we created this uh NexJS project, this page.txs will be the default one. Once we run the application, you will get to know much better. U next we have the node module which contain all the packages to run our application. We are not going to touch to this particular folder at any time. Then we have the public folder in which we are going to store all our images, assets, font, everything. And when you want to use it, you can directly uh refer that. You don't need to provide a complete path. Once you added inside the public folder, NexJS will automatically uh access this public folder without providing a complete path. You'll get to know much better later on once we start implementing. Then we have the next doconfig.ts ts file which contain a configuration related to the NexJS application. Then we have package.json file which contain the name version of your application the script to run build your application the dependency along with their version and some de dependency with their version as well. Whenever you add any third party or any library you will see all this dependency um mentioned under this package.json. Now package lock is used to maintain this version across a multiple places. For example, once you share your project on a GitHub and someone is using this package lock will help us to maintain the version as well. Then we have post CSS for the Telwin CSS configuration and TS config for the TypeScript related configuration. Now simply open a terminal, click new terminal and it will open the terminal inside your VS code. That's cool, right? You don't need to open external command prompt or terminal over here. Simply type command npm rundev and which will run your application. Click enter and then you will see your application is running on URL localhost on a port 3000. Simply open that inside the browser and then your application is now running. Now this is the default page as I mentioned earlier. Right now, if you make any changes to your page tsx file as this is the default page, then you will immediately see the changes inside the browser. For example, let's remove this line of code and here we'll say hello world and then save it. And as soon as we save, you will see that's reflected over here. Pretty cool, right? Now let me remove all of this code and then we want to simply add a do tag inside this we'll say h2 tag and then we'll say subscribe to tube gur. So guys if you did not subscribe to our channel please do subscribe and then save it. Once you save without refreshing your browser you will you will see the changes that we just made inside this page.tsx file. Pretty cool right? So that's how you can create your NexJS application and run it very first time. So we covered this both the point. Now the next step is to install a shaden UI component library. Now in order to install this library simply go to the shad cen ui.shadc.com. Now this is very popular UI component library among the developers. Recently they introduced many new components as well. Simply go to the documentation. Here you will find the installation step. Select the NJS and then you have to follow step byep guide. Only you need to run this command called npx shadian latest initialize in it which will initialize the shaden inside your project. So we'll add a new terminal. We paste this command and then say yes to the proceed. Right now I'm getting this error that's because of the permission inside the Mac OS. So now I will start with this sudo and then say yes. And once it started it will ask you the base color. Okay, for your shaten components you can select anything. In this case I will select a zinc and then it will install the uh shadian inside your project. Say now the interesting thing once you install this shad CN you'll see some of the files get added to your project the very first file which is a component.json JSON which contain a configuration related to your shadian where you will find a tailwind where the telvin CSS variable is saved the base color theme and everything and the lip contain the configuration again obviously we are not going to touch to this lip folder at any time. Now the next step is to use the shaden component. Now whichever shenan component you want to use it you can simp simply select that one and then you can copy the install command. So in this case for example if you want to use a button component simply you can copy and paste it and then it will uh install the button component for you. But if you want to install all the component at once simply you have to type npx chats at latest add. Now once you hit enter it will ask you which component you want to install. If you want to install all just click a to select all and then click enter. Now it will install all the shaden component directly inside your project. So from next time you don't need to install each of the component one by one. Once it's done you will see a very new folder called components. And inside this UI folder you'll see all the component by the shad CN which you can directly use it. You can always refer a shad CN uh documentation in order to use this component. For example, if you want to use a dialog component, then you can see the example how it looks, how it works and then you can use any of this uh use case like to import the uh import statements to use the basic example and then you can modify according to your requirements. So obviously once we start developing our application you will get to know much better as we all now install the shaden UI component library. Now the next part is how to use this shad CN UI component library and that's pretty easy. Simply for example if you want to use a button component simply uh type a button here it automatically give you the suggestion as well that is trying to import it from the component/ UI/button click hit enter and then you can type any text and let's save it as soon as you save you will see the button is now rendering on the screen okay in such a way you can um use any of this component. Now, next thing is to update the of application font and that is again very straightforward. Simply go to the layout.tx here. If you see it already using some of the default font but I'm not going I'm not going to use this. So, I will delete this font and make sure to remove it from here as well and then save it. Now, whichever font you want to use, you can just first search on a Google font. Okay, go to the Google font. Uh just search the font which you want to use. Okay, now in this application I'm going to use this outfit font which is my favorite. Once you know know the name of the font, simply inside this layout import this font. So I will say from NexJS sorry uh it need to be a Google font. So we'll say next /font/g Google. Okay. And then give the font name. As soon as you start typing it will give you the suggestion and select the outfit. Once you define the font which you like you have to initialize that font. So we'll say constant outfit is equal to outfit you need to write a subset and then we'll say Latin. Okay. Now I will also open this. After that you need to assign that font to this body. So over here we'll say class name and inside this we'll say outfit dot class name and then save it. And now if I refresh you will see now font is changed to this outfit. That's how easy you can update the font. Now you can search any Google font which you want and then just replace this import initialize this font and then simply apply it. That's how easy it is. Okay. Now guys, if you have any question uh inside the chapter one, let me know in the comment section. Now in the chapter two, we are going to set up a database. So for that one, we are going to use a convex DB and then we'll learn how to create your first table inside the convex database. Convex DB is one of the popular database among the developers as it provide a lot of different feature like realtime update type safety and you can easily integrate to any number of frameworks. Not only that but convex also give you the option to write a server functions store your files and the most important the chrome job functionality inside of one uh database. Everything you will get inside the convex and the most interesting thing is free to use as well. Now simply go to the developer documentation. I will put the link in the description. Make sure to click on that so that you will jump onto the specific page. Inside this documentation make sure to select a nej as our framework and then we have to follow few step in order to install the convex. Then inside the terminal over here, uh very first thing we need to do is to install the convex. So we'll copy this command. Just make sure to paste and then it will install the convex for us. After installing the convex, you can run the convex by using this npx convex dev command. And obviously if you don't have project created you can uh create a new project or you can choose existing project as well. Now there are two option to do that. Now in order to create this project directly from the convex you have to log to the convex open a dashboard in a new tab and then once you jump onto this uh dashboard here you have option to create a new project. Give the project name. So here we will say AI agent builder platform and simply create this project. Once the project is created you will see currently you are under development mode. Okay just keep like this because once we finish developing our application we'll change this to a production mode that also I'm going to tell you how to do that. Currently we don't have any table or anything. Okay. Now once you uh set up and created this project, let's head back to our VS code. And now we'll select an option called choose existing project. Here you might need to login if you did not log into the convex through this terminal. Now I will also make sure to open this documentation side by side. Then make sure to select the project. So in our case is AI agent builder. So maybe it's down. Once you hit enter, you will see your convex function is ready. Okay. Now after installing this convex, you will see a new folder get added called convex. And inside this we have this underscore uh general fun um folder and inside we have this API data model server and many other files. You don't need to touch to this any of this folder. But when you want to create a table or write any convex function we'll write inside this convex folder. Now the next step is to wrap your application inside the convex provider. Now we are not going to follow this uh step exactly as it is but directly jump onto the step seven where you need to create a convex client provider. So let's copy this name go to the app folder and inside this app we'll create a file called convex client provider. We'll copy the content as it is and we'll paste it here. Okay. And then save it. Now over here if you see we have this next public convex url. When you initialize your project convex automatically create a file called.local file and inside this you will find a convex deployment id and the convex url as well which is all automatically set when you run the convex devo command. Okay. And this is uh the convex client provider. Now the next step if you scroll down you need to wrap the children inside this convex client provider. Okay. So copy this line then go to the layout.tsx file and then I'm going to paste it here. Make sure to import this convex client provider by pressing command space on Mac or control space on um Windows. You will get the suggestion and then hit enter. If you see the import statement is added. Let's save this one. And now you are ready to use the convex. Now very first thing that we need to do is to create the table. Okay, as we finished this convex DB setup, now we'll learn how to create a table inside the convex. For that one, we'll go to the convex folder. Inside this convex folder, we'll create a file named schema. So over here, we'll say schema dot TS inside the schema simply write export default and we'll say define schema then whichever table you want to create you can give the table name we'll say for example user table and then we'll say define this table after that you need to provide a column which you want to add you don't need to provide an ID column because convex automatically add you the unique ID for you. Okay. So here we'll say name of the user. So for name uh you need to provide a type for this column. So we'll need to end uh input this convex/ value which contain all of the type. So we'll say v dot string then I will add a email. Then again this is of type string. Then the image URL if you want to import again this is of type string this is completely optional if user has any subscription right so you can add a subscription again this is of type string and one more I think important thing we can save the token okay so I will give the token of type number and that's all we need it and then save it. As soon as you save it, you can make sure your convex function have don't have any error and it's saying the convex function is ready and that's all. It automatically update your database with a new table even though if you make any modification inside your uh table like adding a table or removing table and save it at automatically update your database. Now let's go to the convex database and let's refresh this and boom if you see we have this brand new table called user table. Obviously we don't have any record but in order to check which are the column is available inside the user table. Simply click on this add and inside this you'll get you will get the schema. See over...
  </details>
  *YouTube: Build & Deploy AI Agent Builder Platform using NextJs, React*

**pSritFeoYFo** (score:54) Matt Pocock (2026-01-12) [35,546 views, 1,508 likes]
  Frontend is HARDER for AI than backend (here's how to fix it)
  https://www.youtube.com/watch?v=pSritFeoYFo
  Highlights:
    - "There's a weird problem with AI coding agents at the moment, which is that they are way better at writing back-end code than they are at writing front-end code."
    - "If you don't know, a skill is kind of like something you would put in your agents.md file, except the agent can pull it in on demand when it needs to."
    - "On the back-end, you can write some code, right?"
    - "We can hook up these same things here to the feedback loop, but they're all textual, right?"
    - "If you've ever tried using an LLM for this, then you will find it is not very good at handling that stuff."
  <details><summary>Full transcript (1193 words)</summary>
  There's a weird problem with AI coding agents at the moment, which is that they are way better at writing back-end code than they are at writing front-end code. I'm not talking about producing like front-end slop, like basic marketing pages and just, you know, the demos that you see people one-shotting. I'm talking about complicated user interactions, multi-step forms, annoying animation things. If you've ever tried using an LLM for this, then you will find it is not very good at handling that stuff. The reason for this is that the feedback loops that the LLM is using are way more precise on the back-end than they are on the front-end. On the back-end, you can write some code, right? And then that code is often tested by more code. The outputs of those tests are written in text, and the entire loop, the entire environment is encoded in text. Even the information about the documentation that you're using is written in text, and the LLMs are really good at writing and understanding text. The difficulty comes when you start adding visual elements to this. A feedback loop in a front-end, if you've ever been a front-end developer, you know this, is to write some code, then look at the UI to see what changed, go back, write a bit more code, look at the UI again. In other words, as humans, we're not just looking at text, we're also taking in visual information. We're able to use our innate design sense and get a feel for what is supposed to be where and try out user interactions ourselves. And in fact, we're not just processing images, we're processing video as well. We're trying to feel how things scroll, how an animation feels when we hover over it. In other words, so much of this feedback loop is visual, not textual. And this is a problem for a lot of AI coding agents. With a back-end feedback loop, you can add unit tests, you can add linting, you can add type checking. You can add these in a pre-commit hook to make sure when the coding agent commits its code, it's going to see the errors come up if there are any. We can hook up these same things here to the feedback loop, but they're all textual, right? We still need a visual element to make the AI coding agent understand what it's done, and whether what it's done actually works. I've been working with AI to rebuild my blog recently, and it kind of looks like this. Pretty basic, but I quite like the minimalist design. I'm going to get Claude Code to do some QA for me. I'm going to ask it to double-check if light mode and dark mode are working okay on the home page, and check if all the content is rendered acceptably on both. And then just going to pass it my local server, which is localhost 5175. What it's going to do here is it's going to check the home page both light and dark mode using Chrome DevTools. It's first tool uses to spin up a new page, and I'm going to say yes and don't ask again. It's now going to take a screenshot of that with a full page true, and it's took a screenshot of the full current page. It spotted, I don't see a visible toggle, the page likely uses system preference. Let me emulate dark mode using a strict script to check the prefers-color-scheme behavior. And it continues in this vein doing more screenshotting, checking all the pages, adding the dark class didn't change anything. The site is using media-based query dark mode, very good. It finally figures out that we're using media prefers-color-scheme dark, and then it manages to emulate that, takes a screenshot, both modes look good, and cleans up and provides a summary. The way I've got this set up is with an MCP.json inside my project, which is running the Chrome DevTools MCP server, pointing it at a running browser URL. Alongside this, I also have a skill inside here for Chrome debugging. If you don't know, a skill is kind of like something you would put in your agents.md file, except the agent can pull it in on demand when it needs to. This one, for instance, says, "Before using Chrome DevTools MCP, you must launch Chrome in headless mode with remote debugging enabled." And that's it, that's the entire setup. So, alongside the text-based feedback loops like testing, linting, type checking, we've now got an actual browser that the LLM can go and use. And having the browser there means the LLM is much more like a human coder. They get access to all of the feedback loops that you have, too. And bear in mind, the LLM is not actually writing tests for this stuff or writing end-to-end tests you need to run on every commit. It's doing the same kind of testing that human devs do, which is ad hoc, testing the feature that they built actually works. Now, I posted this on X, and I got some really great responses. Basically saying that the Playwright MCP, which was what I was using at the time, is not very good, and there are several fighting in the same space that are maybe a little bit better. The one that Damian pointed out here, Dev Browser, is pretty good, but I've not yet tried it. And the other one that he pointed out, which was this Playwriter, I think. Yeah, this thing here, another MCP, is also supposed to be really good, too. What people say about the Playwright MCP server and the Chrome DevTools server, too, is that they're pretty context-hoggy. They take up a lot of tokens in order to pump their screenshots back into the LLM, and their MCP servers are configured in a way that makes them really context-heavy, like really detailed tool descriptions, lots of different tools, that sort of thing. You can also use Claude Code directly with Chrome using the Claude Chrome extension. I can't use it at the moment because I'm on WSL, and it's not supported on WSL. The last thing to say here is that this front-end feedback loop makes AFK AI coding a lot more powerful. If you're looping an AI, for instance, in the Ralph Wiggum setup, then plugging a browser into your front-end or full-stack work will be such a massive improvement, because without it, your LLM is essentially flying blind. It can't see the execution environment in which the changes are being made. So, I've noticed an enormous improvement when I plug in a visual feedback loop into this setup. If you dig this, then you will dig all the stuff I'm putting out on AI Hero. There are a ton of free tutorials and free guides that help you start building with AI, and especially putting AI into applications, which is where things start getting really gnarly and really fun. But thank you so much for watching, and I will see you in the next one.
  </details>
  *YouTube: Frontend is HARDER for AI than backend (here's how to fix it*

**gfK19KrpHJk** (score:48) WorldofAI (2025-10-30) [18,697 views, 601 likes]
  Convex Chef: NEW AI Full-Stack Builder! Opensource Lovable, V0, & Bolt.new Alternative! (FULLY FREE)
  https://www.youtube.com/watch?v=gfK19KrpHJk
  Highlights:
    - "Convex is also an open-source reactive back-end platform that provides real-time databases, serverless functions, and built-in identification for seamless full-stack development."
    - "Cuz this is something that will be leveraging Convex's powerful APIs, which is well-suited for AI code gen."
    - "Next, we can go ahead and use the NPX Convex dev {dash} {dash} once command, and this is where we're going to now set up the Convex project."
    - "So, this is where we're going to start with our Convex project."
    - "You can create a Convex account completely for free."
  <details><summary>Full transcript (2013 words)</summary>
  A while back, we had taken a look at one of the best AI full stack app builders. A tool that could turn a single prompt into a fully functional app with a built-in database, zero config auth, file uploads, real-time UIs that actually look beautiful, and even a background workflow. That tool was called Chef by Convex. And now Chef is completely open source thanks to the Convex team. For those who do not know, Chef is one of the very few AI app builders that can actually code out a back-end fluently, not just front-end mock-ups. Convex is also an open-source reactive back-end platform that provides real-time databases, serverless functions, and built-in identification for seamless full-stack development. They have now made Chef fully open source so that you can easily get started with it, which is what we're going to be showcasing today. But just take a look at the quality of Chef. You can simply tell it to create a Notion clone, and in this case you can be as descriptive as possible, provide it a couple of different files, or even mock-ups, and then you can send in this prompt. It's a super simple process, and this is where Chef is going to be able to take in your prompt and fully configure it and build it out. This doesn't just generate the UI, it doesn't generate some sort of mock-up, it is going to build it complete with production-ready full-stack components with an identification system, a built-in database, as well as so that you can have it monitored in the back-end. But now the best part is is that this is a full-on open-source tool that you can work with locally. And this way it is going to allow you to build anything on your local computer. You're going to have a zero-config authentication system set up. You have file uploads, as well as the real-time UI with background workflows. And the secret sauce is definitely Convex's open-source reactive back-end and database designed to make your life super easier. Cuz this is something that will be leveraging Convex's powerful APIs, which is well-suited for AI code gen. And you can see right here that it is even running terminal-based commands and executing a plan to create this Notion clone. And this is where you can actually get a live preview of what it's actually generating in action. There we go. We have a full-on functional app based off of the natural language prompt that was sent in. We have a full-on identification system that was fully configured and built that is linked to our database, which we have a live preview of the different sorts of accounts that are registered. We can manage the functions, files, as well as all the logs with our app. But now we can sign in anonymously, and we can actually take a look at the Notion clone that it generated, which looks actually amazing cuz we can go ahead and add in a document title, and then we can make it public and create it, and we can now initialize the document, and you can work with it like you would with Notion. So, for example, you can add in different sorts of headings, you can add in quotes, you can even add in different tables, and all of this was fully coded out in a couple of seconds. This is what you can now do completely for free and with an open-source framework that is locally hosted. Cuz you can link this up with local models, and then you can have it running on your local computer to generate any sort of full-stack app. And the funny thing is is that this Notion clone was actually generated completely for free off of their cloud version, which you can access as well. And this is something that will also help you prototype quite rapidly. But now say if you want to download the code, you can click on download code, you can share it, you can even deploy it directly from the Chef website. And you can see our site is deployed, and now we can access it through this new Convex link. And I have mentioned this before in another Convex video, but you also have recipes, and essentially this is where you can add new tool integrations inside your Chef app. So, for example, if you want to make a collaborative text editor, you can add that recipe, and it's going to be able to thoroughly add it within your app. For example, you can even enhance the prompt, you can even use other recipes like adding an AI chat, you can even add an file upload feature, or a full-on text search feature as well into your application using these recipes. But the beauty of Chef now is that you can now have it locally installed, which is where we're going to be now showcasing how you can set this up fully free locally. It's super simple and easy to get started. Head over to the GitHub repository, and just make sure you have a couple of the prerequisites fulfilled. Make sure you have Node.js installed, as well as Git. And once you have these prerequisites fulfilled, we can proceed forward with installation. To install this, I will be using WSL, the subsystem for Linux on Windows, cuz it's a bit trickier to install this cuz NVM only works with Mac and Linux. You can find an alternative where you can install an NVM package for Windows, but it's a bit complicated. So, if you want to use the WSL installation method, you can follow through with this example. So, what you can do first is go ahead and clone this repository, open up your terminal, and simply go ahead and paste in the Git clone command to clone the Chef repository onto your computer. Once it has finished cloning, we can then head over into the Chef directory by typing in CD Chef. Once inside the Chef directory, you can copy the NVM install command, and this is where we're going to now set up the local environment. So, simply go ahead and use the NVM install command. We're going to now use the NVM use command. Then you can simply use the NPM install command to install and unpack all the packages. And now use the PNPI command. Now simply go ahead and create a .env.local file by simply copying the echo command and pasting it in within your terminal. Next, we can go ahead and use the NPX Convex dev {dash} {dash} once command, and this is where we're going to now set up the Convex project. So, this is where we're going to start with our Convex project. Simply go ahead and create one if you do not have one already. You can create a Convex account completely for free. This is for the database back-end setup. So, simply go ahead and create that with your Google account if you haven't already. Now, since we have already created one, we can click on log in, and then this is where we can now select our device name, and then we can authenticate this. After authenticating your account, you can simply go ahead and create a new project. You can give it a name, so we're just going to name it AI Chef, and then we can have it either locally deployed, or we can have it cloud deployed. So, this depends on wherever you want to host your project. I'm going to be hosting it locally for now, so we're going to click on the local deployment. And once that is done, it is going to set up and download the back-end for Convex. And you can see that it is now fully set up. What you can do next is use the NPX Convex dashboard command, and this is where you can set up the Convex deployment. So, you can open up the Convex dashboard, go to the settings, and set the environment variables. Then you can set the following environment variables for like brain host, or the Convex OAuth client ID, as well as the API keys that you can set within the env.local file. So, I'm going to do that for both of these two different subsets. And just to showcase how you do this, after you have opened up the dashboard, which you can get the link from, you can then head over to the settings, go over to environment variables, and then provide all the four different environment variables that they require you to paste in. In the same manner, you want to simply go ahead, and you want to go into the Chef folder, and you want to set your API keys over here. So, in this case, if you want to use Anthropic, you want to set your Anthropic API key, Google, OpenAI, You can also use local models, so if you want to use something like Ollama, you can configure that within this section over here. Next, you want to run the NPX Convex dev command in one terminal, and the PNP run dev command in the other one. So, now what we can do is head over to this localhost link, and we can open it up within our web browser. So, now we can access the open-source Chef version off of our computer. It'll take a couple of seconds to load, but then you can start sending in any sort of prompt. In this case, I'm simply going ahead and building a shared expense app, a Splitwise clone. And you can see that it is rapidly prototyping and working on building this out by coding out all the different components of this app. And that is how easy it is. And there we go, we have the shared expense app that was fully generated in a couple of minutes. You also have it so that you can actually rewind different checkpoints. So, this way if you want to go back to the previous generation, you can actually click on the rewind button, which should show up. In this case, it doesn't show up since I only sent in one prompt, but if you had another prompt, you can click on that rewind button to go back to the previous checkpoint. In essence, this is the capability of Chef. If you like this video and would love to support the channel, you can consider donating to my channel through the Super Thanks option below. Or you can consider joining our private Discord, where you can access multiple subscriptions to different AI tools for free on a monthly basis, plus daily AI news, and exclusive content, plus a lot more. This, in my opinion, is the best full-stack open-source tool that's out there that you can easily get started and build in real-time with reactive applications thanks to Convex powering it. It is going to easily help you in various ways to build various sorts of back-end functions autonomously for your full-stack app. So, I highly recommend that you take a look at this with the links in the description below. Make sure you go ahead and give them a star cuz this is something that is fully open source and they are giving you a lot of value. So, I highly recommend that you go ahead and take a look at this with the link in the description below. But, with that out guys, thank you guys so much watching. Make sure you go ahead and subscribe to the second channel. Join the newsletter. Join our private Discord. Follow me on Twitter. And lastly, make sure you guys subscribe. Turn on notification bell. Like this video. And please take a look at our previous videos cuz there's a lot of content that you'll truly benefit from. With that out guys, have an amazing day. Spread positivity and I'll see you guys really shortly. Peace out fellas.
  </details>
  *YouTube: Convex Chef: NEW AI Full-Stack Builder! Opensource Lovable, *

**YM9nvDYvEiQ** (score:48) Convex (2025-08-04) [5,368 views, 138 likes]
  How to Build Realtime AI Agents with Convex Components
  https://www.youtube.com/watch?v=YM9nvDYvEiQ
  *YouTube: How to Build Realtime AI Agents with Convex Components*

**F6gsiIZzNII** (score:47) Convex (2026-03-31) [4,181 views, 173 likes]
  AI agents love Convex. Here's why.
  https://www.youtube.com/watch?v=F6gsiIZzNII
  *YouTube: AI agents love Convex. Here's why.*

**u1yDq2uu-dk** (score:47) Convex (2025-03-07) [7,411 views, 123 likes]
  Cursor & Convex MCP Server (Model Context Protocol) - How AI Agents Can Query Your Database
  https://www.youtube.com/watch?v=u1yDq2uu-dk
  *YouTube: Cursor & Convex MCP Server (Model Context Protocol) - How AI*

**3Ydg6McrCf0** (score:43) Convex (2025-06-09) [3,773 views, 100 likes]
  Building AI Agents and Workflows for Full-stack Apps with Convex Components
  https://www.youtube.com/watch?v=3Ydg6McrCf0
  *YouTube: Building AI Agents and Workflows for Full-stack Apps with Co*

**UoJjeyQR66s** (score:40) Convex (2023-08-24) [6,785 views, 169 likes]
  We Made AI Town's Backend. Here's How.
  https://www.youtube.com/watch?v=UoJjeyQR66s
  *YouTube: We Made AI Town's Backend. Here's How.*

**TtrqXt9ZeyQ** (score:38) Convex (2026-03-30) [3,363 views, 157 likes]
  Convex projects just got a BIG AI upgrade!
  https://www.youtube.com/watch?v=TtrqXt9ZeyQ
  *YouTube: Convex projects just got a BIG AI upgrade!*

**tUKMPUlOCHY** (score:38) Convex (2025-06-11) [11,802 views, 295 likes]
  Powerful AI Apps Made Easy with the Agent Component
  https://www.youtube.com/watch?v=tUKMPUlOCHY
  Highlights:
    - "Okay, so first things first is I'm going to assume you have a convex project running."
    - "Um, if not, check out the convex docs for how to get started there."
    - "Now, let's install our agent component with bun install@convexdev/ aent."
    - "Once we've done that, let's create a file in our /convex directory called convex.config.ts."
    - "Then on the client side, we could call it with just like any other convex mutation like this."
  <details><summary>Full transcript (2545 words)</summary>
  The aging component is a great way to quickly get started building a powerful AI app. It's designed to get you going really quickly, but have all the knobs and dials allowing you to customize it to your own requirements. If that sounds like something you're interested in, then stick around as I'm going to go through all the things you need to know to get started. How to install it, what its key concepts are, and how to use it most powerful features. So, grab yourself a lovely cup of tea. Let's get started. Okay, so first things first is I'm going to assume you have a convex project running. Um, if not, check out the convex docs for how to get started there. Now, let's install our agent component with bun install@convexdev/ aent. Once we've done that, let's create a file in our /convex directory called convex.config.ts. And this is uh how we're going to set up our components that we're going to use in convex. So here we define our app and then we import our agent and then tell the app that we're going to use that agent component. So now we're set up. Let's uh let's define our first agent. So agents are our top level object that we're going to use to represent our assistant that's going to assist the user with their prompt. So we can give it a name, choose what chat and text embedded models we want to use. Uh and then we're going to use well for this demo we're going to use GPT 4.1 mini and text embedding 3 small. Um, and then we can give it some instructions. So, these are sometimes referred to as our system prompt, and they should just be like general instructions that are going to be cons constant for the length of your interaction with the assistant. Finally, we're going to provide max steps. Um, this is because the agent uh is going to call tools in a loop. So, you could potentially get into an infinite loop scenario. So, you just kind of want to bound the number of steps it can do here. And there are lots of other options that we don't have time to go into here, but uh check out the agent component docs if you'd like to learn more about some of these. So now that we have our agent, we can use it to actually create a thread like this. So here we've created a convex mutation that uses the agent to create a thread. Um and we give it an optional user ID so that we can restrict access to the thread and its messages. Then on the client side, we could call it with just like any other convex mutation like this. So here we have a very simple app that either shows the thread if we have it otherwise it creates it um via this button on click here and once it's created we then just set the thread ID locally and then that means that the thread's then shown. So now when we try this out, we should see that there's no thread created initially until we click the button. Uh and then we see nothing as the thread has been created, but we don't actually have any implementation yet for the thread. Okay, so now we have a thread. Let's use the agent to send a message to it. We'll do this in the very simplest of way for now, but we'll make this quite a bit nicer later on with a bit of streaming. So here we define an action that's going to take our thread ID in and the prompt from the user and then we're going to ask the agent to continue the thread with our given thread ID. So this continue thread function here is kind of a helper wrapper that's basically some nice syntax sugar that we can use on the next line to call out to the model with the prompt from the user. So now that we have that our action, we can then call it from the client in our uh thread view component like this. So here we define a basic UI that takes a prompt from the user here and then when we click the button we trigger the form submit here which in turn calls the send message to agent action here and then once we get the results back we can save it into the local states here and then present it onto the screen down here. All right so let's see this in action now. So, when I type a message like, "Hello, please tell me what is the best kind of tea," we get an answer back. Super nice. And notice if I do a follow-up prompt here, the agent is going to know when it responds what I'm talking about. That's because the agent component has automatically pulled in the relevant message history for us and added that as part of the call to the a to the LLM when we call it. And this is super nice because doing this yourself is more work than you might think, particularly as the message history starts getting longer and longer and longer. Okay, this is nice so far, but you will have noticed that as we ask additional questions, it just simply replaces our previous questions. So, it would be nice if we had a more chat-like interface. So, we can see our previous messages in here. Now, although you could manage this yourself by writing into the convex database the result of the generate text call, you don't need to as the agent component already is saving the messages for us. So, we just need a way to retrieve those messages and then show them on the screen. So, let's define a message list component and put it in our thread view component here. Inside there, we're going to use a special use thread messages hook from the um agent component import. And the use thread messages hook is going to take in a thread ID and some options and a convex query that's going to you that we're going to use to retrieve the messages from the server. So let's have a look at that query now. So here we have a convex query that takes a thread ID and some pageionation options. Um we have to supply those because our react hook the use thread messages hook on the client is going to return a back a pageionated list of results. So it needs to be supplied with the current cursor position from the page. Then we're going to use our agent that we defined before to list the messages for a given thread. We can also optionally here define some other parameters to filter out the kind of messages we get back. But for now, let's just keep it simple. Okay. Then back on the client, we can just add some basic UI to show the messages. And now when we run it, we should see our messages from before, including the ones we sent and the ones from the assistant. And we can send another message like this one. And we will see our message show up. And then we should see the assistant message show shortly after. All right. Now, our agent is good. But it's a little bit limited in what it can do. It can only respond based upon what's inside of its own head or whatever we tell it. It would be nice if the agent itself could pull in a bit more information by itself. Well, thankfully this is really easy to do with the agent component using its tool calling ability. So if we hop back to our agent definition from before, now if we add a tools object here, we can define any number of tools that we want our agent to have access to. So for example, let's create a tool that is I don't know going to get the current weather. So you'll notice here we use this create tool function that the Asian component provides us which takes an object with an description args and a handler. And the AI is going to be able to see the description and the args definition. So make sure they're accurate um if you want to give the LLM the best chance to have success here. And the handler uh looks very much like a convex action function. So we have a context and an args here which is going to contain our location uh field. So using the context we could do a query to look something up in the convex database or mutation if we wanted to change something. But in the case of this get weather function I think we just want to do a fetch to some remote service uh like this. But for the purpose of this demo we're just going to mock it out with something like this. So, if the user asks what the weather is in England, it's going to be rainy because, let's be honest, it probably is. Um, otherwise, it's going to be sunny. Okay, let's try it out. Okay, now, so when I ask for what the weather is in England, I am told correctly that it is rainy. And then if I ask about what the weather is in Australia, then of course it's sunny. That's great. But notice that there are these additional empty messages showing up here. If I just quickly hop back into the code and log out the messages from the use thread messages hook here and then back to the browser, we can inspect the messages in the console and have a look what's going on. If I open up this one here, we can see that we have our user message at the start that we sent and then at the end we have the final message from the assistant. But then also in the middle here, we have these messages which are looking at them are the tool calls that the agent used to look up the weather. So what we could do if we didn't want to show these empty boxes here which aren't very nice is we could update our react code to show some so tool calls and the tool call results in line here but I think for this demo it might be simple if we just exclude them from our messages. So we could do that easily if we hop back into our list thread messages query. Now, if we just update the list messages options object to add the exclude tool messages, true. And then we hop back into the browser, we should see that those empty messages now disappear. Nice. Looking good now. Okay. So, one last thing I noticed before is when we asked it to generate a story, we had to wait for the entire result to come back before we showed anything on the screen. What it would be nice is if we could do what chat GPT or T3 chat or something else like that does, which is stream the results back before the entire message is finished. So we can show the user something on the screen. Well, fortunately, Convex has also got it back here with the agent component. It has a stream messages feature. Let's take a look. So if we head back to our messages list component, we can add another parameter to the options object for the use messages right here. So we want to stream the messages in as soon as they become available. So we set stream to true here. But if we do that, we're going to get a TypeScript error. That's because we need to update our list thread messages query to add uh some stream args. So let's do that now. Okay. So these are needed um to do the proper streaming uh syncing. And so once we've done that, we can then also add another call down here to the agent to grab some of that streaming information. And then we uh return it all together with the pageionation from the list messages. Now if we return back to the browser and we again ask for a long story and nothing happens. It doesn't stream. That's because we still need to change one more thing. So, let's hop back into our send message to agent action and instead of our generate text call here, let's replace it with the stream text call instead, which is going to kick off the streaming process and then persist into the database thanks to this save stream deltas argument here. By the way, you can control how big the deltas are and thus how many writes the database the component is going to do. if you change this true here to an object with a different chunking strategy. But to keep this demo simple, let's just leave it as true for now. And the final thing we need to do is call consume stream so that the we wait for the entire stream to finish before the action finishes. Otherwise, we'll get dangling promises. Okay, let's hop back into the browser now and then ask it again for a long story. And now, nice, we should see it come streaming in. Very, very nice. Okay, well I think I'm going to have to leave it at this point as I don't want this video to get too long, but there is just so much more I could talk about the agent component. I mean, just under the topic of streaming alone, I could talk about optimistic updates or HTTP streaming or stream smoothing or hybrid streaming, just so much. And then more general topics like from the aging component, we could talk about image uploading or rag or MCP or human in the loop or the playground UI, which by the way is amazing. um or workflows or advanced filtering and storage options or just so much more. So, if you're keen to see more about these kind of advanced agent component topics, then make sure you are subscribed to the channel as I'll definitely be doing a follow-up video. And let me know in the comments down below which topic you'd like to see me cover next and I'll see what I can do. And if you can't wait for me to do that, then definitely check out the agent component documentation and its associated examples as it really does do a great job of explaining things. Okay, well that's it for me for now. I hope you really enjoyed this video. And if you did and you want to check out more content like this, you might want to check out my other video here, the Mikebot V2 video I did recently. I also used the agent component in that to condense 20 years of blog posts down to a chatbot that I embedded onto my blog. It was a heap of fun. Okay, until next time. Thanks for watching. Cheerio.
  </details>
  *YouTube: Powerful AI Apps Made Easy with the Agent Component*

**FL_UiNpGgEg** (score:36) Jack Herrington (2026-02-10) [3,493 views, 107 likes]
  How to Build a Multi‑User AI Chat App with Convex
  https://www.youtube.com/watch?v=FL_UiNpGgEg
  *YouTube: How to Build a Multi‑User AI Chat App with Convex*

**exWpG7szUMM** (score:36) Web Dev Cody (2025-04-08) [9,794 views, 275 likes]
  My favorite approach to coding complex AI agent workflows
  https://www.youtube.com/watch?v=exWpG7szUMM
  *YouTube: My favorite approach to coding complex AI agent workflows*

**Wi50AVUvbgo** (score:33) Income stream surfers (2025-11-08) [2,688 views, 55 likes]
  CONVEX.DEV + CLAUDE CODE = MY NEW FAVOURITE AI DEV STACK
  https://www.youtube.com/watch?v=Wi50AVUvbgo
  *YouTube: CONVEX.DEV + CLAUDE CODE = MY NEW FAVOURITE AI DEV STACK*

**XwJFEd1Lnmo** (score:33) Convex (2025-02-25) [705 views, 26 likes]
  AI vs Backend Development: Fullstack Benchmarking for AI Coding Agents
  https://www.youtube.com/watch?v=XwJFEd1Lnmo
  *YouTube: AI vs Backend Development: Fullstack Benchmarking for AI Cod*

**hiptYyfndxc** (score:28) Convex (2025-11-21) [1,336 views, 41 likes]
  How Blueberry built the best AI Marketer for B2C brands on Convex
  https://www.youtube.com/watch?v=hiptYyfndxc
  *YouTube: How Blueberry built the best AI Marketer for B2C brands on C*

---
**Sources:**
  ✅ X: 9 posts
  ✅ YouTube: 16 videos (4 with transcripts)
  ⚡ Xiaohongshu: Xiaohongshu API unavailable or not logged in - start xiaohongshu-mcp and login (base: http://host.docker.internal:18060)
  ⚡ Web: assistant will use WebSearch
