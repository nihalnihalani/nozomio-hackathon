## Research Results: Tensorlake sandbox compute agents

**Date Range:** 2026-04-09 to 2026-05-09
**Mode:** both
**OpenAI Model:** gpt-5.1-codex-mini

### Reddit Threads

*No relevant Reddit threads found for this topic.*

### X Posts

**X5** (score:77) @diptanu (2026-05-02) [45likes, 9rt]
  New guide for creating massively parallel browser infrastructure with Chrome DevTools Protocol (CDP) and @tensorlake sandboxes.

1. Create a sandbox with the ubuntu-vnc image. It has Chrome pre-instal...
  https://x.com/diptanu/status/2050683352492147146
  **

**X9** (score:70) @diptanu (2026-05-01) [20likes, 4rt]
  . @tensorlake sandboxes now support GUIs for enabling computer-use agents. Spin up a sandbox with VNC + XFCE pre-installed so the agent can interact with the GUI using tool calls.

You can also hook i...
  https://x.com/diptanu/status/2050231449757045164
  **

**X8** (score:59) @diptanu (2026-05-01) [10likes]
  Live demo for Computer Use agents on @tensorlake - https://t.co/midcIIbtWn

The app itself runs in a sandbox, which is in a suspended state and boots up when you visit the URL! 

Auto resume in action...
  https://x.com/diptanu/status/2050233943757344943
  **

**X1** (score:57) @computesdk (2026-05-08) [8likes, 3rt]
  🚨🚨🚨

Benchmark Friday is back! 

A huge shoutout to @tensorlake our latest provider added to the sandbox benchmark!

You can tell they've been working hard! https://t.co/PSff4zbwSP...
  https://x.com/computesdk/status/2052839097215885425
  **

**X12** (score:51) @diptanu (2026-04-27) [22likes]
  Tensorlake sandboxes are the only API that supports dynamic resource allocation for sandboxes and can boot them in under 300 milliseconds. This lets you create a sandbox with the right amount of resou...
  https://x.com/diptanu/status/2048839648034877544
  **

**X2** (score:50) @DominikTornow (2026-05-07) [4likes, 2rt]
  Resonate runs any process durably in the background, in bash, docker, or a sandbox as a Durable Promise.

Check out Claude Code vibing a script and Resonate coordinates the execution in a @tensorlake ...
  https://x.com/DominikTornow/status/2052463478233870649
  **

**X15** (score:49) @diptanu (2026-04-17) [11likes]
  We made a product decision @tensorlake to not queue sandboxes if we don't have the resources to spin one up. This guarantees that users either get a sandbox or don't within 300ms-1s. We maintain warm ...
  https://x.com/diptanu/status/2045192838250668522
  **

**X17** (score:48) @diptanu (2026-04-13) [16likes, 1rt]
  Pumped about all the changes we made to @tensorlake sandboxes in the last few days.

A full memory + disk snapshot of a running sandbox now pauses the VM for under 100 ms. This makes it possible to ta...
  https://x.com/diptanu/status/2043713406444970459
  **

**X22** (score:47) @diptanu (2026-04-11) [18likes]
  I am curious why most sandbox infrastructure APIs (other than @tensorlake, sprites, exe dev) still have timeouts associated with sandboxes. 

If users want their sandboxes to be suspended when a proce...
  https://x.com/diptanu/status/2042812503672131883
  **

**X14** (score:44) @diptanu (2026-04-17) [6likes]
  We are planning on rolling out dynamically allocated disks for @tensorlake sandboxes over the weekend!
 
This is huge for coding agents working with C++, Go and Rust as you can get arbitrary sized dis...
  https://x.com/diptanu/status/2045230624525717581
  **

**X10** (score:42) @nicgimbal (2026-04-30)
  @toddsaunders @conductor_build @Railway @nikunj Been loving @tensorlake for document extraction, they are now doing a full sandbox thing that looks interesting...
  https://x.com/nicgimbal/status/2049837641139458212
  **

**X11** (score:42) @cooltxeel (2026-04-30)
  Tried something fun tonight, put my favorite small VLM (Mississippi 800M from my previous work) into a @tensorlake sandbox, runs great on cpu. 😁 https://t.co/mby8fbMYsS...
  https://x.com/cooltxeel/status/2049694225407541516
  **

**X7** (score:36) @ibuildthecloud (2026-05-02) [1likes]
  @diptanu @browser_use @tensorlake Definitely. The VNC stream is interesting. Is that pretty unique to your offering. I haven't notice any other sandbox offering that. Obviously you can doing yourself ...
  https://x.com/ibuildthecloud/status/2050471351526547868
  **

**X6** (score:36) @ibuildthecloud (2026-05-02) [1likes]
  @diptanu @browser_use @tensorlake the only thing that is weird now is that instead of the agent being some random python code calling the sandbox, we are now just throwing the agent into the sandbox. ...
  https://x.com/ibuildthecloud/status/2050477240606199858
  **

**X13** (score:36) @tensorlake (2026-04-21) [5likes, 1rt]
  Suspend = pause and resume the same sandbox
Snapshot = save state and reuse it to create new sandboxes
Many providers use these terms differently — which is where confusion starts.
Tensorlake supports...
  https://x.com/tensorlake/status/2046707355543630015
  **

### YouTube Videos

**HwvyBw83sco** (score:15) Multiversal Ventures (2026-02-27) [17 views, 1 likes]
  Infrastructure for Agentic AI - Durable execution, sandboxing, and end of serverless bottlenecks.
  https://www.youtube.com/watch?v=HwvyBw83sco
  Highlights:
    - "People are building um deploying like coding agents like on our platform and um um uh it it combines a couple of different capabilities um that that agents need today."
    - "Like when you run an agent today, it's inevitably running um on a secure sandbox."
    - "You can't necessarily do the same thing with LLM and it can creates a lot of deviation in the way the agents are thinking."
    - "The coding agent can deploy tensor lake on tensor lake and then &gt;&gt; uh we expose logs over API we obser we expose traces over API and then coding agents can deploy uh understand like the behavior of the application fix any problems and so on."
    - "&gt;&gt; I think the other piece here is um file &gt;&gt; Um temporal and you know like every other system that came out like 10 years back or six years back they were built for orchestrating microservices."
  <details><summary>Full transcript (5000 words)</summary>
  The lighting tells otherwise. &gt;&gt; Yeah. Hey, hey, hey. It's another Friday and it's time for an autonomous rift and we have a special guest. I'm Ash. That's Karthik. And we have Diplanu here with me. Diplanu, who are you and what do you do? &gt;&gt; Yeah. Um, I'm the founder of this uh company called Tensor. Um, we are a serverless platform for uh developers to build and deploy agentic applications on. Um and um um uh people are building um agentic data applications on our platform. People are building um deploying like coding agents like on our platform and um um uh it it combines a couple of different capabilities um that that agents need today. One of them is like sandboxing abilities. Like when you run an agent today, it's inevitably running um on a secure sandbox. Um and then we we the runtime itself like has uh orchestration abilities. Um agents work through tools and uh when an agent works through tools like you inevitably need some um orchestration capability to start tools, stop tools, um start a sub agent, uh getting results back from sub agent. So we built a platform where all of these things are uh becomes like really easy like for developers building these systems. &gt;&gt; Awesome. &gt;&gt; You know uh there are so many things I want to unpack there. Uh but before we go there maybe you can give us your background as well like you know why are you building this startups are hard and you've been a founder now. So give us your background. Why you why are you doing this? &gt;&gt; Yeah I I've been working on cluster schedulers for a long time. I used to work on a clusteruler called Apache MSOS back in the day. Um we we built this to powers spark um around 2012 and 13 and um uh then I built a containeruler when I was at Netflix uh before docker like pre-docer um that was powering and I believe like it still powers Netflix's uh microser infrastructure on AWS &gt;&gt; um we were running like tens of thousands of containers uh around that time on on many thousands of machines &gt;&gt; um and then I was one of the early engineers at Hashi Cororp where I started another clusteruler called Nomad. &gt;&gt; Uh Nomad is used by hyperscalers um Fly.io and probably like Verscell back in the day was using it &gt;&gt; um and um and and and so on. And then I worked on worked on um Facebook's machine learning platform called FBlearner for a couple of years &gt;&gt; as a tech lead of the speech team uh back back in the day in Facebook and um couple of years back when LLMs got uh into the scene. Um I thought the the most interesting use cases of LLMs at that time was uh reasoning on unstructured data and data infrastructure for a long time has been um like a u has been powered by SQL or like a SQL oriented &gt;&gt; uh user experience right &gt;&gt; so you want to process a lot of audio or you want to process a lot of video like you're going to uh spark and then you know you are writing UTFS like which would do like a atomic operation in your pipeline and then your pipeline is basically like a complex SQL expression &gt;&gt; and &gt;&gt; right &gt;&gt; that's that's very different than software engineering right that gets into the realm of data engineering and my thesis was that &gt;&gt; if you're building an LLM application you would want to build uh applications in Python or TypeScript but have the same abilities that spark or any other data infrastructure tool would do. So I wanted to build a serverless data infrastructure so that de software engineers can build data injection applications for LLMs without feeling that they need to learn distributed &gt;&gt; Right. Right. &gt;&gt; I think that's a really uh that's a really salient point. I mean originally we were going to talk about stuff like document extraction but I think the very you know key part that we're also seeing in finer right now it's all coming down back down to data engineering and distributed data engineering &gt;&gt; and definitely having a platform yourself a platform like yours to make that serverless is such a crucial need because if you look at every other serverless platform out there it was built for web applications right it was built for &gt;&gt; hey I'm going to send you a request you're going send me a response back, you get 500 megabytes or whatever it is for Lambda to um to respond back. But that's not the case anymore with LM, right? Like they need long running jobs and audio and video ironically where the most closest analogous uh data engineering pipelines. Um if you look at, you know, AWS Lambda today, we actually started with AWS Lambda as an email response server, but it couldn't hold on to the context long enough for the agent to actually respond correctly. So this is a this is a very interesting problem today and how the dynamic is shifting now where people want realtime tokens back as it's thinking and it's reasoning but the final result itself requires a lot of packaging of everything around the LLM before you're actually able to deploy that. So let's talk a little bit about why serverless and you know why not keep that as a server fold. Um yeah so um first of all like you touch in like some very interesting points here about about lambda being uh lambda and the previous generation of yeah of serverless platforms designed for like web applications. I think yeah what has happened uh in the last year or two is that the line between data engineering and and and and software engineering or like web applications has blurred. &gt;&gt; Yeah. Yeah, &gt;&gt; if you're building an agent and want the agent to do deep research, it has to like go through a lot of documents, it has to browse the internet, get a lot of data. And so now these applications are interactive, right? Like people are using them like they use web application, but behind the scene they're not just reading a few KB of JSON from a database. &gt;&gt; Yeah. &gt;&gt; Right. they're reading uh MBs or in some cases like tens of ms of data like from the internet and then dumping them in the file system and then reasoning about the data and then responding right. Um second like timeouts these applications cannot time out. If if they time out then the session the agentic session is over. &gt;&gt; Yeah. So building a serverless platform which never times out, building a serverless platform that has durable execution built in like where every operation is guaranteed to be complete was is super important now than ever. &gt;&gt; So I think this requires like re-engineering like the serverless platforms from the ground up &gt;&gt; and also the tools right the tooling that we needed to do regular application. But going back to one topic that you mentioned right it's not just the the timeouts are not only from uh the fact that they can't time out because sometimes timeouts will happen even upstream with the LLM providers &gt;&gt; the real problem there at least the way we see it is a unit economics problem &gt;&gt; right every timeout will require a rerun of everything all the tokens that needed to happen especially if you don't have good casting built in so traditionally what used to happen for longunning jobs is you would do something like stream processing Okay, for something like video or audio and then you would cast as you went. But because those algorithms were deterministic in nature, there was only one solution out. You can't necessarily do the same thing with LLM and it can creates a lot of deviation in the way the agents are thinking. If you cast every single step and then all of a sudden you had a timeout and you need to rerun the last step, has the context changed? Should it go back and get new data? That is like some of the deep challenges that we're also seeing in the agentic side of things right like in fin one of the things that we do is scenario scenario scenario planning where we're look helping loan officers to investigate what are the different type of calculations that can help present the best financial story. So in that concept right as the user is talking to the application the context is changing let's imagine a timeout happened in a reasoning model while the user is talking do we take the latest information do we take the most last information and that really that the tooling for that and the scheduling for that I don't think has been really built out or thought out at an agentic level maybe crew AI is somewhere close but they are talking more about squads of little agents talking to each other but from um you know business first use case I don't think anyone's really thought about durable LLM execution right while the context is live right this this is something that is completely missing in the ecosystem today have you have you also seen that or do you know uh other folks doing that &gt;&gt; yeah I think I think the the pain point that you're mentioning right like is is pretty uh like well uh accepted pain point and so u some LLM providers are now coming up with like a more stateful uh API for talking to LLMs. Mhm. &gt;&gt; The other thing that I see is that um the other thing that I see is like durable execution engines like getting in the agentic runtime itself, right? Like where &gt;&gt; people once they have done the MVP, right? They have proven that the agent works, then they rewrite their applications with an durable execution engine. &gt;&gt; Uh and what is durable execution engine? It's basically a mechanism for uh application to restart from where they crashed. And the technique that we use under the hood is like you know pretty straightforward like in principle right it's that &gt;&gt; every function uh or every every task in your workflow basically is it goes to a database and then record the sequence in which like these functions are called. So if anywhere like you know your system crashes they can you can like replay from the top and then get back to where you were before assuming that the software is deterministic right as in like there is no like random number or &gt;&gt; and that's the key part right yeah that's the key variance right there yeah &gt;&gt; yeah and yeah &gt;&gt; no I think I think that's a great point like the you hit the nail on the head and um I think there's something exciting happening in the world of uh you know uh on the AI side we're starting to realize that the LM's weakness also is indeterminism. There's a there was originally this concept and I say used to always say this is that helpful hallucination. And I'm like that's fine but if you want to process a document hallucinations are not necessarily helpful in no matter no matter what way it will happen right if you're doing a data extraction and precise calculations on it it doesn't matter right so uh we've been hearing a lot about batch invariant kernel which basically means that when it replays a function that happens to determine det uh uh depend on an LLM it will give you the same result as long as the input is the same right and that marriage of like durable execution with batch invariant kernels will also allow us to leverage caching in an intelligent way because remember like back in the web API sort of concept the first thing people did was okay don't update the data keep the data as close to the application as possible don't update that data until absolutely necessary you can't do that with LLM because every call is different right now &gt;&gt; um so I see once the batch and variant kernels come into play for some of the larger foundational models they're going to have to redo their entire training pipeline which is why it hasn't yet. But once that comes in, then you can not only get durable execution, but you can also get deterministic results back from the same LLM call, which I think is like the last big missing piece right now for to solve this problem on the agentic scheduling side. &gt;&gt; Yeah, what we are pushing for is that you know we our platform like has has some key innovation right like here one of the things that we have done is um we build durable execution in the runtime itself right. So when you run a function, &gt;&gt; so talk about how that different than something like temporal or airflow. &gt;&gt; Yeah, good question. So durable execution engines like force you into a framework, right? &gt;&gt; Um so what they do is for example, temporal has this dichotomy of workflows and activities. A workflow &gt;&gt; allows you to write write a sequence of steps and uh you're not supposed to like have any side effects in it. Any sort of side effects is supposed to be happening in activities. Um, so if you're making an IO or something like you would put this in an activity. &gt;&gt; Um, so in in that world like if you're making an LLM call, right, you would wrap the LLM call in an activity. So now like when your workflow runs and if it crashes, it can basically replay and and and and u uh and skip the steps like which it has already done like calling an LLM. So you you're guaranteed to get the same responses again, right, for the same even the same input, &gt;&gt; right? What we have done is that we think like that is like too cumbersome, right? Because that takes away away from your domain, right? So if you're building an application in Python or TypeScript, &gt;&gt; you want to program the way you program on your laptop or anywhere, right? You do not want to like change your mental model of like how you want to build build an application. &gt;&gt; And that stems from this this foundational problem that programming languages and runtimes from programming languages, they are not stateful. M &gt;&gt; so when you when you run a function like it's a it's you know you get the response back there is no concept in a programming language runtime that the function can be durable or the function can be stateful &gt;&gt; you have to build state like in your application for something like that to be straightfor &gt;&gt; so what we have done is like we added some annotations in Python and Typescript which lets you tell the runtime that this function needs to be durable. So whenever like we discover that a function is getting called which with a particular annotation which says that it needs to be durable we under the hood we manage the the the checkpointing of like the input and output of the function and also record the sequence. Right. Right. &gt;&gt; Because we we run the we control the runtime. We know like what is the um sequence in which like functions are called. Mhm. &gt;&gt; So now for a programmer like they feel like they're running a code that they had written like a normal program that they have written and it works but on lake it suddenly becomes durable if you wanted to. &gt;&gt; So this is interesting because &gt;&gt; um go ahead. &gt;&gt; No no no go ahead I'll come back to it later. the the reason why this annotation approaches. So we use temporal right like uh at uh fino uh because we have a lot of signals that also go out to integrations to different systems and temporal was you know the offering that we have at the time um and I hear your problem about like having to switch my mental model to think like how temporal requires you to think. uh totally fine. Uh but one of the things that's interesting about the annotation approach and uh this is also was in the news recently is the ability to add the annotations to existing code bases, right? What if you had like a really big module that you probably spent $50,000 maintaining as an enterprise on any given basis, a lot of algorithmic imagine you just took that out and replaced it with an LLM that was fine-tuned with the inputs and output for the last 30 years cuz these softwares have been around for a while, right? and you say you know these are the different con uh requirements that need to change it becomes a prompt you turn that into a durable annotated function in the language as is Python language as is and theoretically if we're thinking a little bit future out I know we're not there necessarily yet but theoretically that would be the quickest way to bring AI into an existing application without changing a single line of code aside from an annotation so the unlock there is massive and this is Why I would chuckling because you know recently Anthropic 808 IBM 10% or 12% of their stock price because they said now they could do Coobo programming and I'm like I'm sure you could generate Coobo tokens but can you make it work durably for a bank in the transaction speed that you need to right a very different thing. So uh you know IBM's looking pretty uh looking like a very good price point right now is my point. Hey, just disclaimer, we we're not we're not recommending stocks here. Just want to &gt;&gt; we don't recommend anything whatsoever. Don't ever listen to me. I have not done great in that regard. &gt;&gt; So, I have a question for you. U so this comes to me u because as I sell finero uh and also like you know pretty much every engagement that we're in uh we run up against what what I call as um uh classic CTO's that say you know I can do this now with cloud core. I can do this uh by myself. Um and and we know Karthik and I know that very deeply like why some of these things won't happen for several reasons but maybe you can answer that question as a neutral party here but also like maybe you want to answer that from a uh you know from your your product and platform point of view because you're solving these problems and and why should like every company not be trying to build it on their own and actually like you know look at data platforms like yours. &gt;&gt; Yeah. So I think our programming model was built for agents to code. We don't write a single example ourselves for our platform. Whenever someone wants to see um a demo of tensor lake like we ask them like what is your use case? &gt;&gt; Uh I write this prompt on on a coding agent I pointed to tensor lick's docs and comes out like a perfectly working demo that gets deployed on tensor lake. The coding agent can deploy tensor lake on tensor lake and then &gt;&gt; uh we expose logs over API we obser we expose traces over API and then coding agents can deploy uh understand like the behavior of the application fix any problems and so on. &gt;&gt; Yeah. So we designed our platform like from the perspective that agents are going to deploy software on tensor like not humans. That is like &gt;&gt; and second um uh on like why someone should use tensor lake or not try to build it themselves is that &gt;&gt; um we guarantee reliability right in an in an age where software is cheap like what is not cheap is &gt;&gt; uh guaranteed reliability and uptime &gt;&gt; and being on the hook like for your uptime right so if you deploy your software on tensor lake like we are on the hook for making sure it works exactly like as as we have promised The other thing is that uh building a cluster scheduleuler is very complex right like for example &gt;&gt; um we support up to like 15,000 machines like on a single cluster and upward of like couple of million um containers or sandboxes like on our on our platform. M &gt;&gt; um at scale um uh at scale everything like breaks apart and over time like we have improved the how the algorithms for bin packing how the algorithms for like finding like a place for like a sandbox to run that wouldn't get done on like day one right like even &gt;&gt; even if like we used AI today to write this whole thing and we use a lot of AI in our company &gt;&gt; um like we that we wouldn't get to like the amount of reliability like we have today on day one. &gt;&gt; I'm sure like someone can build something like this um on on with with an AI agent much quickly u than than you would be able to do like two years back. Uh but uh but the but there is a lot of lessons to be learned along the way that only comes in &gt;&gt; when when you deploy software in production. &gt;&gt; No 100%. I think uh we're noticing that as well on finer's side, right? When we first started, we're like, hey, let's just dump a bunch of documents into whatever favorite LLM API it is and let's start ask uh tell it how to reason stuff by building a rag engine and then give us an answer for qualified income. And you know, of course, that was the very first version. It looked like it worked, but now you know we have a test gauntlet of about 200,000 different type of, you know, combinations of income that we test all the way from extraction reasoning to uh reason to make sure that whatever recommendations it's making are and and those all came from very hard lessons where when to use an LLM but also when to know not to use it very appropriately and I think that's financial algorithm but I I like the way you're also mentioning from a scale algorithm perspective, Right? That's not a problem that we want to solve at a vertical AI company. That's definitely a platform problem that needs to be solved. And what I'm seeing over and over again with a large hyperscalers is that they will add features of this but they will focus on integrating to their existing technologies first. Right? Like if you see Amazon come out, if you see Google come out, you wouldn't see something like uh browser base come out in Amazon very quick, right? Browser base is a gentic webcwler. uh we've used it at some at some point you wouldn't see that pop up in Amazon as an offering as a managed offering probably if I were to bet and you guys can record this probably in the next 5 years you wouldn't see it why because there's tremendous rework that needs to be done of all of the bin packing problem that you were talking about and just for everyone's context like for for some of the folks that are watching and don't necessarily understand why bin packing is important think about it this way let's say you have a 100,000 documents you need to process and you need to keep that context that's passing from one area of your code to another area of code where LLM can process the right piece of it correctly. You need to compress that information so that the right information runs otherwise you're moving everything around all the time and that becomes very very difficult to manage and especially gets very expensive. So today the way it manages that people put into a database they run a query against it. But if you're doing unstructured data you don't even know what the schema is. You don't know the format. You don't even know what document it is. You don't even know if it's a PDF. It could be an Excel C. It could be a doc. So if you're sloshing that data around without thinking about stuff like bin packing which is a very crucial algorithm and dur durable execution. It it doesn't play out right off the bat. And I and I buy your thesis if you're not thinking about it from an agentic problem. you don't think about really putting effort into keeping state like our approach to state with temporal now is that we write it and then we read it and we write it and we read it between every single large uh sections of our calculations but that's expensive as well right especially as we do larger and larger uh financial uh document processing on our system so um yeah I'm I'm very excited to see you know how how you going to continue to solve those type of problems the scale is not only just volume but it's also So depth of the data you could have one PDF that's 10 kilobytes but then as you keep reading it the context grows so massively right that you have no way to measure that. &gt;&gt; I think the other piece here is um file &gt;&gt; Um temporal and you know like every other system that came out like 10 years back or six years back they were built for orchestrating microservices. &gt;&gt; Mhm. And um um microservices are a different beast than an agentic application. Like today like if you use clot code and ask clot code to analyze um like a document uh most likely some of the context is going to be offloaded into the file system. Um so now imagine like serving five different requests um uh using this agentic application. You need like five different sandboxes running like your agent so that they don't share the file systems. &gt;&gt; Like if you and I are like talking to the agent simultaneously, you need like two copies of agent so that the file systems are isolated, &gt;&gt; right? &gt;&gt; Software was not built with that assumption that they would need file system access. Mhm. Mhm. Mhm. &gt;&gt; Another thing that we have done in our engine is that every time like a function starts, you can say that I want an isolated sandbox for this function. &gt;&gt; Right. &gt;&gt; Right. So we mix durable execution and sandboxing together to make like a perfect execution engine for agents like where it's still durable. The function calls are still durable but they also get a file system. So if you're doing something agenting inside a function, you you are guaranteed that you'll have an isolated file system and data is everything and everything related to that. Oh, okay. That makes sense. See, I I hadn't really understood why the file sandboxing was important, but if I were to think through it from a document processing use case, uh we get a file, we get a file uploaded. &gt;&gt; Yeah, &gt;&gt; we'll break up all the pages. We'll classify what those pages are and then moving forward I uh everything in that workflow will focus on only that page. There is a stitching aspect which is kind of like a map reduce where &gt;&gt; you'll do each single page. you'll then find out which pages are related to each others and which ones aren't and then that gives you a final like concept of a document. So in that case where does sandboxing really fit in? Sandboxing would fit in like wherever you want it to fit in right like so let's say like you have let's say um a request come in where you are processing like personal uh information right like from from every user &gt;&gt; and it's like it's like their personal documents and so on. So what you would want ideally is that the the the sandboxes like which are uh interacting with the document to be isolated for every request for every agent session. &gt;&gt; So even if you do map and reduce like you want those map to go onto sandboxes related to that request &gt;&gt; So you almost like want like a stateful group of sandboxes serving like different parts of your application and then &gt;&gt; but they're all running on the same file system. &gt;&gt; Yeah. Exactly. And then they are like there are different sessions like running like on different sandboxes at that at the same time. &gt;&gt; Wow. That's a super hard problem because you have to make sure the right data is in the right place at the right time before the function is even ready to go and you have to load that up correctly. So that's pretty impressive that you you guys have learned how to solve that problem. &gt;&gt; Yeah, we we built a a containeruler, right? Brand new containeruler, right? like which &gt;&gt; which sits on top of like a which sits under an application scheduleuler. &gt;&gt; So whenever the application scheduleuler like gets a new request, it's going to tell the containeruler that give me this container to run this function. &gt;&gt; that function now starts...
  </details>
  *YouTube: Infrastructure for Agentic AI - Durable execution, sandboxin*

---
**Sources:**
  ✅ X: 20 posts
  ✅ YouTube: 1 videos (1 with transcripts)
  ⚡ Xiaohongshu: Xiaohongshu API unavailable or not logged in - start xiaohongshu-mcp and login (base: http://host.docker.internal:18060)
  ⚡ Web: assistant will use WebSearch
